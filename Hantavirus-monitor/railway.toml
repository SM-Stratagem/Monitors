[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "node scripts/railway-entry.mjs"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
